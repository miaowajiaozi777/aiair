# AI气味播放器与气味共创社区（UGC）移动端 UI 系统设计规范

## 快速说明（如何打开演示）

- 本文件是 **UI 设计规范文档**（用于设计与研发对齐），不是可运行 App。
- 若需直接查看界面演示，请打开同目录 `ui-demo.html`。
- 若出现“拒绝连接”，请先启动本地服务（推荐脚本）：

```bash
bash 启动演示.sh
```

浏览器访问：

```text
http://localhost:4173/ui-demo.html
```

也可直接双击 `ui-demo.html`（`file://`）离线查看。

---

## 1. 产品定位与设计原则

### 1.1 产品定位
一款融合 **AI 气味生成、气味播放设备控制、社区分享与互动** 的移动端应用。
目标体验：
- 专业可信（可商用落地）
- 情感沉浸（技术与感官融合）
- 轻盈克制（不过度炫技）

### 1.2 视觉关键词
- 现代
- 平静
- 高级
- 专业
- 感官科技

### 1.3 核心设计原则
1. **信息优先**：内容可读性高于装饰性。
2. **柔和层级**：通过轻透、模糊、阴影构建深度，不依赖重边框。
3. **一致圆角**：主要容器圆角 `20–24px`，组件形态统一。
4. **低饱和高级感**：森林绿 + 海洋蓝为主色，暖日落为强调色。
5. **微交互动克制**：柔和渐隐、轻位移、短时反馈，避免花哨。

---

## 2. 视觉基础系统

### 2.1 色彩系统（低饱和、柔和渐变）

#### 主色（森林绿 / 海洋蓝）
- 主绿 `#4F7A6A`
- 深绿 `#3E6356`
- 主蓝 `#4E7D95`
- 深蓝 `#3E647A`

#### 强调色（暖日落）
- 暖橙 `#E79C6B`
- 柔珊瑚 `#D9876E`

#### 中性色（背景与文本）
- 背景浅灰 `#F3F4F2`
- 背景暖米 `#F4F1EA`
- 卡片底 `rgba(255,255,255,0.62)`
- 一级文字 `#253238`
- 二级文字 `#5E6C73`
- 分隔线（极轻）`rgba(37,50,56,0.08)`

#### 渐变建议
- 主渐变 A（森林到海洋）：`linear-gradient(135deg, #4F7A6A 0%, #4E7D95 100%)`
- 主渐变 B（海洋到深海）：`linear-gradient(135deg, #6F96A9 0%, #3E647A 100%)`
- 强调渐变（暖日落）：`linear-gradient(135deg, #E79C6B 0%, #D9876E 100%)`

### 2.2 光影与玻璃拟态

#### 模糊层（Glass）
- 背景：`rgba(255,255,255,0.52 ~ 0.68)`
- 背景模糊：`backdrop-blur(14px ~ 20px)`
- 内高光：`inset 0 1px 0 rgba(255,255,255,0.45)`

#### 阴影层
- 卡片阴影：`0 8px 24px rgba(26,40,46,0.08)`
- 悬浮阴影：`0 14px 30px rgba(26,40,46,0.12)`
- 按钮阴影：`0 6px 16px rgba(78,125,149,0.20)`

### 2.3 圆角体系
- 页面主卡片：`24px`
- 内容卡片：`20px`
- 输入框/按钮/标签：`999px`（胶囊）
- 图片：`20px`

### 2.4 间距体系（8pt）
- 最小间距：`8`
- 常规间距：`12 / 16`
- 模块间距：`20 / 24`
- 大区块间距：`32`

### 2.5 字体与排版（仅简体中文）

#### 字体建议（无装饰）
- 首选：`PingFang SC`
- 备选：`HarmonyOS Sans SC`、`Noto Sans SC`

#### 字重
- 标题：`Semibold 600`
- 正文：`Regular 400`
- 强调数字：`Medium 500`

#### 字号层级
- 页面大标题：`28 / 34`
- 区块标题：`22 / 30`
- 卡片标题：`17 / 24`
- 正文：`15 / 22`
- 辅助信息：`13 / 18`
- 标签与按钮：`14 / 20`

---

## 3. 组件系统

### 3.1 顶部导航
- 左侧：可选返回/菜单
- 中间：页面标题（如“气味社区”）
- 右侧：筛选、通知或设备连接状态
- 高度建议：`56px`
- 支持浅玻璃背景 + 轻模糊

### 3.2 搜索框（圆角满宽）
- 高度：`48px`
- 圆角：`999px`
- 背景：半透明白 + 模糊
- 占位文案：**“搜索气味配方、场景、用户…”**
- 左侧图标：搜索
- 右侧可选：语音输入

### 3.3 趋势标签（胶囊）
- 高度：`32px`
- 内边距：`12px 14px`
- 示例文案：`木质` `雨后` `助眠` `海风` `咖啡馆`
- 选中态：主色低饱和填充 + 白色文字
- 未选中态：低透明底 + 深色文字

### 3.4 气味卡片（瀑布流/双列）
- 圆角：`20px`
- 图片比例：`4:5`（可变高形成瀑布流）
- 内容结构：
  - 气味名称（单行或双行截断）
  - 作者：`@用户名`
  - 底部互动：点赞数、评论数
- 视觉增强：依据类别叠加轻微色彩光晕
  - 木质：绿褐光晕
  - 海洋：蓝青光晕
  - 花香：暖粉光晕
  - 柑橘：暖橙光晕

### 3.5 胶囊主按钮
- 文案示例：`播放` `扩散` `立即创作`
- 高度：`50px`
- 圆角：`999px`
- 背景：主渐变
- 文本：白色中粗
- 按下态：亮度降低 6%，阴影收敛

### 3.6 玻璃质感滑杆
- 轨道高度：`8px`
- 轨道：半透明白 + 轻发光边缘
- 进度：主渐变
- 滑块：`22px`，带柔和外发光
- 值显示：右侧数字百分比（如 `68%`）

### 3.7 底部导航栏
- 菜单：`首页 / 社区 / 创作 / 我的`
- 容器：浮动玻璃胶囊（高 `68px`，圆角 `24px`）
- 激活态：主色图标 + 文案
- 未激活态：低对比灰蓝

### 3.8 信息分区卡
- 标题 + 内容 + 操作
- 分区间距清晰，避免拥挤
- 不使用重边框，使用浅底色和阴影区分

---

## 4. 页面级设计规范

## 4.1 首页 / 发现页

### 页面结构
1. 顶部标题：**气味社区**
2. 满宽圆角搜索框
3. 趋势标签横向滚动
4. 双列卡片流（或瀑布流）
5. 底部导航栏

### 卡片内容（必须项）
- 圆角图片
- 气味名称
- 作者名：`@用户名`
- 点赞数、评论数

### 布局参数建议
- 页面左右内边距：`16px`
- 卡片间距：`12px`
- 顶部到搜索框：`12px`
- 搜索框到趋势标签：`12px`
- 标签到内容流：`16px`

### 交互建议
- 下拉刷新：轻阻尼
- 卡片点击：缩放 `0.98` + 透明度过渡
- 列表加载：渐显 + 轻上移 `8px`

---

## 4.2 气味详情页

### 页面结构
1. 顶部大视觉图（沉浸式）
2. 气味标题与创作者信息
3. 点赞数 / 评论数
4. 主操作按钮：**播放 / 扩散**
5. 交互式浓度调节滑杆（显示百分比）
6. 气味结构模块：**前调 / 中调 / 后调**

### 视觉重点
- 背景渐变呼应当前气味主题（例如木质偏绿、海洋偏蓝）
- 主操作按钮始终高可见
- 结构信息分区清晰，风格极简

### 模块示例
- 前调：佛手柑、青草
- 中调：白茶、铃兰
- 后调：雪松、麝香

### 交互建议
- 播放按钮点击后出现柔和脉冲
- 浓度滑杆拖动时实时更新百分比
- “扩散”状态显示轻微动态光环

---

## 4.3 气味创作页

### 页面标题
**创作气味**

### 页面结构
1. 顶部标题与保存操作
2. 模块化控制面板（前调/中调/后调）
3. 每个模块包含滑杆 + 数值
4. AI 推荐配方模块
5. 预览与发布入口

### 模块细节
- 前调 / 中调 / 后调各自独立玻璃面板
- 每条滑杆旁显示当前值（0–100）
- 可提供“重置”与“锁定比例”辅助功能

### AI 推荐配方模块
- 标题：`AI 推荐配方`
- 子文案：`根据当前配比，为你生成更平衡的气味方案`
- 推荐卡片内容：
  - 推荐名称
  - 配比建议（前/中/后）
  - 一键应用按钮

### 网格与对齐
- 采用 4 列基准网格（移动端）
- 关键控件左对齐
- 数值区域等宽，保证专业感

---

## 5. 动效系统

### 5.1 动效原则
- 短时（`180ms–320ms`）
- 缓动：`ease-out` 或 `cubic-bezier(0.22, 1, 0.36, 1)`
- 动作幅度小，不抢内容

### 5.2 页面切换
- 轻微渐隐 + 位移（Y 轴 8–12px）
- 时长 `260ms`

### 5.3 微交互
- 按钮点击：轻缩放 + 阴影变化
- 标签切换：背景色过渡
- 滑杆拖动：滑块柔光增强

### 5.4 可选背景粒子
- 仅在首页和详情页开启
- 粒子透明度低（`< 10%`）
- 运动速度慢，避免视觉疲劳

---

## 6. 文案系统（统一简体中文）

### 6.1 导航与主入口
- 首页
- 社区
- 创作
- 我的

### 6.2 首页文案
- 标题：气味社区
- 搜索占位：搜索气味配方、场景、用户…
- 标签示例：助眠、雨后森林、深海、木质调、清晨柑橘

### 6.3 详情页文案
- 主按钮：播放 / 扩散
- 模块标题：气味结构
- 分项：前调 / 中调 / 后调
- 浓度：当前浓度 68%

### 6.4 创作页文案
- 标题：创作气味
- 模块：前调强度、中调强度、后调强度
- AI 区块：AI 推荐配方
- 操作：一键应用、保存草稿、发布到社区

---

## 7. 可访问性与可用性
- 文本与背景对比满足可读性（正文建议至少 4.5:1）
- 可点击区域不小于 `44px`
- 滑杆可同时支持拖动与点击定位
- 动效可在系统“减少动态效果”下自动弱化
- 图标需有文字补充，避免仅靠颜色传达状态

---

## 8. 设计交付清单（用于研发落地）
1. 颜色与透明度变量（含明暗场景）
2. 字体与字号 Token
3. 间距与圆角 Token
4. 组件状态（默认/按下/禁用/加载）
5. 三大页面高保真稿（首页、详情、创作）
6. 页面切换与微交互动效标注
7. 文案字典（全简体中文）
8. 适配规范（iPhone 标准宽度优先）

---

## 9. 设计 Token（可直接给开发）

```json
{
  "color": {
    "primaryGreen": "#4F7A6A",
    "primaryBlue": "#4E7D95",
    "accentSunset": "#E79C6B",
    "bgSoftGray": "#F3F4F2",
    "bgWarmBeige": "#F4F1EA",
    "textPrimary": "#253238",
    "textSecondary": "#5E6C73",
    "glassFill": "rgba(255,255,255,0.62)"
  },
  "radius": {
    "card": 20,
    "panel": 24,
    "pill": 999
  },
  "spacing": {
    "xs": 8,
    "sm": 12,
    "md": 16,
    "lg": 24,
    "xl": 32
  },
  "font": {
    "family": "PingFang SC",
    "title": 28,
    "section": 22,
    "body": 15,
    "caption": 13
  },
  "motion": {
    "fast": 180,
    "normal": 260,
    "slow": 320,
    "easing": "cubic-bezier(0.22, 1, 0.36, 1)"
  }
}
```

---

## 10. 结论
本规范通过统一的视觉语言、模块化组件与克制动效，建立了一个 **“技术理性 + 情绪感知”** 的移动端 UI 系统。整体在简体中文语境下具备高可读性、强一致性和商业可实施性，能够支撑 AI 气味播放器与 UGC 社区的长期产品演进。
