# 演示打不开（拒绝连接）怎么办？

你遇到“拒绝连接”，通常是因为**本地服务没有启动**，或端口地址不对。

## 最简单方法（推荐）
在仓库目录运行：

```bash
bash 启动演示.sh
```

然后打开：

```text
http://localhost:4173/ui-demo.html
```

> 若 4173 端口被占用，可换端口：

```bash
bash 启动演示.sh 8080
```

并访问：`http://localhost:8080/ui-demo.html`

## 不开服务，直接打开文件（兜底）
该演示页是单文件静态 HTML，也可以直接双击 `ui-demo.html` 用浏览器打开（`file://`）。

## 常见原因排查
1. 终端没保持运行（启动后马上关了）
2. 地址输错（例如少了 `ui-demo.html`）
3. 端口被占用
4. 远程环境未做端口转发（如云开发容器）

## 文件说明
- `移动端AI气味播放器_UI系统设计规范.md`：设计规范文档（文字规则）
- `ui-demo.html`：可视化演示页面（可直接看界面）
- `启动演示.sh`：一键启动本地服务脚本
